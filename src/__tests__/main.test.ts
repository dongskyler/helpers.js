/**
 * Main test script
 */

describe('Dummy test for now', () => {
  test('Dummy test', () => {
    const input = 'helpers.js';
    const output = input;
    const answerKey = 'helpers.js';
    expect(output).toStrictEqual(answerKey);
  });
});
